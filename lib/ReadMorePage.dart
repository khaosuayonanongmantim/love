import 'package:flutter/material.dart';

class ReadMorePage extends StatelessWidget {
  const ReadMorePage({super.key});

  @override
  Widget build(BuildContext context) {
    
    return Scaffold(
      backgroundColor: Color.fromARGB(255, 255, 211, 134),
    );
    
  }
}