import 'package:flutter/material.dart';

class DetailClub extends StatelessWidget {
  const DetailClub({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.purple[100]
      
    );
  }
}

