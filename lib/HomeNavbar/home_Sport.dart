import 'package:flutter/material.dart';

class HomeSport extends StatelessWidget {
  const HomeSport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        backgroundColor: const Color.fromARGB(255, 252, 186, 208)
    );
  }
}

